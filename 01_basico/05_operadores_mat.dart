
/**
 * Un operador es un simbolo que le dice al compilador
 * que debe de realizar una tarea matematica, relacional
 * o logica y debe de producir un resultado
 */

main() {
  int a  = 10 + 5;
  a = 20 - 10;
  a = 10 * 2;

  double b = 10 / 2;

  b = 10.0 % 3;

  b = -b; // Usado para cambiar el valor de la exprecion

  int c = 10 ~/ 3; //Es una divicion normal pero solo se va a guardar el entero, en este caso 3

  int d = 1;
  d++;
  d--;

  d += 2;
  d -= 2;
  
}